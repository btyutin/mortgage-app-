import * as React from 'react'
import {observer} from "mobx-react";
import {Box} from "@chakra-ui/react";

export const LifeInsurancePage = observer(function ClientMainPage(_props) {
    return (
        <Box>
            Страхование жизни
        </Box>
    );
})